{
  "name": "legwork-web-api",
  "version": "0.0.1",
  "description": "The web server that allows the Legwork client app to communicate with the backtester",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sebastian Rivera",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  }
}
